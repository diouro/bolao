# Read-only credentials for cloning production data into local Supabase.
# Used by: npm run db:local:clone
# This file is ONLY used for pg_dump (read). Nothing writes to production.

POSTGRES_URL_NON_POOLING=
